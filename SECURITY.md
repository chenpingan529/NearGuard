# 安全说明

## 威胁模型

NearGuard 的设计目标是：**只有你的 iPhone 在身边时才自动解锁；你不在时有人操作 Mac 能及时发现。**

| 攻击 | 防护 |
|---|---|
| 录下蓝牙信号后重放（中继 / 重放攻击） | 每份测距报告绑定 Mac 发出的一次性随机数，10 秒过期 |
| 伪造手机 | 报告必须由配对时登记的安全芯片私钥签名，私钥无法导出 |
| 旁路监听配对过程 | 配对码只在二维码中，BLE 上只传 HMAC 证明 |
| 伪造远程指令 | 指令由手机私钥签名，带时间窗口和随机数防重放；发送前需 Face ID |
| 读取保存的登录密码 | 密码只存本机钥匙串，访问控制限定本 App |

## 已知限制

- **中继攻击无法完全防止**：攻击者如果同时靠近你的手机和 Mac，并实时转发蓝牙信号，可能让 Mac 认为手机在身边。随机数只能防止「事后重放」，不能防止「实时转发」。高安全场景请开启定时警戒（期间禁用自动解锁）或关闭自动解锁。
- 自动解锁的前提是「手机在身边 = 本人在身边」。手机被他人拿到并靠近 Mac 时会被解锁。
- 模拟输入依赖辅助功能权限，拥有同等权限的恶意软件也能读取屏幕输入，不在防护范围内。

## 报告漏洞

请不要公开提交 issue。通过 GitHub 的 [私密漏洞报告](https://github.com/chenpingan529/NearGuard/security/advisories/new) 提交，我们会尽快回复。
